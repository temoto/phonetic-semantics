package main

import (
	"github.com/temoto/phonetic-semantics/phonosem"
	"regexp"
)

var reFeature *regexp.Regexp

func ParseFeatures(body []byte) ([]phonosem.Feature, error) {
	regexp.Match(pattern, b)

	features := make([]phonosem.Feature)
	return nil, nil
}

`
		<tr>
		<td>Хороший-Плохой</td>
		<td>1,5</td>
		<td><div style='background: blue; width: 51%; height: 20px;'></div></td>
		<td><span id='blue'>Хороший</span></td>
`

func init() {
	reFeature = regexp.MustCompile("")
}
